/**
 * Create Bulk.
 * @param {Object} ctx - The KOA context
 * @return {void}
 */
const createBulk = async (ctx) => {
  ctx.ok()
}

/**
 * Get Bulks.
 * @param {Object} ctx - The KOA context
 * @return {void}
 */
const getBulks = async (ctx) => {
  ctx.ok()
}

/**
 * Update Bulk.
 * @param {Object} ctx - The KOA context
 * @return {void}
 */
const updateBulk = async (ctx) => {
  ctx.ok()
}

module.exports = { createBulk, getBulks, updateBulk }
